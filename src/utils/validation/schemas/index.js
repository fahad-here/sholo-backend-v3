const EditUser = require('./editUser')
const AddBackTestConfig = require('./backtest')
const RunSimulation = require('./runSimulation')
const CreateAccount = require('./createAccount')
const CreateBotConfig = require('./createBotConfig')
module.exports = {
    EditUser,
    AddBackTestConfig,
    RunSimulation,
    CreateAccount,
    CreateBotConfig
}
